#include<stdio.h>

int main()
{
    const float pi=3.14;
    float r,area,volume;
    printf("Enter value r=");
    scanf("%f",&r);
    area=4*pi*r*r;
    printf("Area of circle is =%f",area);
    printf("\n");
    volume=4/3*pi*r*r;
    printf("area of volume is =%f",vfolume);
    }
